// Locale entry point. Today the blog ships a single (English) catalog; this
// barrel is the seam where locale selection would live if more are added.
import { en } from "./en";

export const strings = en;
